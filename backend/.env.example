# Copie este arquivo para ".env" e gere sua própria chave secreta.
# Nunca faça commit do arquivo .env real.
#
# Para gerar uma chave aleatória:
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=troque-por-uma-chave-secreta-aleatoria
